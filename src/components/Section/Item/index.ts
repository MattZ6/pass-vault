import { SectionItemLabel } from './Label'
import { SectionItemRoot } from './Root'
import { SectionItemValue } from './Value'

export const SectionItem = {
  Root: SectionItemRoot,
  Label: SectionItemLabel,
  Value: SectionItemValue,
}
